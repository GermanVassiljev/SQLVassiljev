1. Определите правильность написания кода. Поставьте в квадратные скобки исправление рядом с ошибкой
CREATE TABLE tabel(
ID int not null Primary key(1,1),
Tabel Nimi varchar (50))

INSERT INTO tabel(Tabel Nimi)
VALUES (Nimi)

SELECT FROM tabel
WHERE ID==1;
2. Как создать ограничение, которое связывает таблицы?
3. Используя исправленную таблицу первого задания, сделайте изменения с ним(Обновите запись и удалите её)
